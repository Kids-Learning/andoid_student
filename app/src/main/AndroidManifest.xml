<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kids_learning">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Kids_Learning"
        android:usesCleartextTraffic="true">
        <activity android:name=".ViewNumericalProblem"></activity>
        <activity android:name=".ViewMaterials" />
        <activity android:name=".Home2" />

        <uses-library android:name="org.apache.http.legacy" />

        <activity
            android:name=".HomePage"
            android:label="@string/title_activity_home_page"
            android:theme="@style/Theme.Kids_Learning.NoActionBar" />
        <activity android:name=".numerical_answer"/>
        <activity android:name=".ViewStudent" />
        <activity android:name=".Login" />
        <activity android:name=".Setip">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>